<div class="col-md-8 col-md-offset-2">
	<h1>Questions</h1>
	<% @posts.each do |post| %>
		<div class="well">
			<h3><%= post.title %></h3>
			<p><%= raw post.body %></p>
			<%= link_to "Read More", post_path(post),
			:class => 'btn btn-default' %>
		</div>
	<% end %>
</div>
